# Run extension locally

Load the extension in your browser (Chrome):
- In Chrome, go to chrome://extensions/.
- Enable "Developer mode".
- Click "Load unpacked" and select your extension directory.

Reload the local extention in chrome://extensions/ and reload the web app as well enable changes in the codebase.


# Privacy Policy
This browser extension does not collect or share any user data